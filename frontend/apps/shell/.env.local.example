# Remote micro-frontend URLs — read at request time by _document.tsx (not baked in at build).
# Copy to .env.local for local overrides, or set as environment variables in your deployment.
# Defaults to localhost dev ports when not set.

CUSTOMERS_URL=http://localhost:3001
WORKORDERS_URL=http://localhost:3002
INVENTORY_URL=http://localhost:3003
BILLING_URL=http://localhost:3004
